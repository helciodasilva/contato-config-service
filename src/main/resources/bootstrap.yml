server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          #uri: file:///E:/dev/workspace/contato-config-repo/
          uri: /app/config-repo
